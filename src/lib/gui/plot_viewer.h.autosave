#ifndef PLOT_VIEWER_H
#define PLOT_VIEWER_H

#include <QWidget>
//#include <QGraphicsScene>
#include <QGraphicsView>

class QGraphicsScene;


class plot_viewer : public QGraphicsView
{
  Q_OBJECT
private:
  QGraphicsScene *m_scene;
public:
  explicit plot_viewer(QWidget *parent = 0);

signals:

public slots:
};

#endif // PLOT_VIEWER_H
